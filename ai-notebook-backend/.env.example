# Example environment variables for StudyLM
# Copy to .env and fill in your values

OPENAI_API_KEY=

# Optional CORS (comma-separated list)
# CORS_ALLOW_ORIGINS=http://localhost:5173,https://studylm.app

# Security: expose API docs only in development (0=off, 1=on)
# ENABLE_API_DOCS=0

# Optional: token used for internal health checks (if not set, falls back to OPENAI_API_KEY)
# HEALTHCHECK_TOKEN=

# Optional tuning
# MAX_CHUNK_TOKENS=750
# VECTOR_STORE_DIR=vector_store
# MAX_PDF_MB=20
# MAX_PDF_PAGES=200
# EMBEDDING_MODEL=text-embedding-3-small
# CHAT_MODEL=gpt-4o-mini
